#include <iostream>
using namespace std;

int pow(intlong long times){
    if(times==0) return 1;
    times--;
    return n*pow(n,times);
}

int    long long n, times ;
    cin>>n;
    cin>>times;
    cout<<pow(n,times);
}